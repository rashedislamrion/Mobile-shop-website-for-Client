# ==============================================================================
# NovaMobile Frontend Environment Variables (Set in Vercel Project Settings)
# ==============================================================================

# Public URL of the backend REST API (Render / Railway)
NEXT_PUBLIC_API_URL=https://novamobile-api.onrender.com/api/v1

# Base URL of the backend server (Render / Railway)
NEXT_PUBLIC_BACKEND_URL=https://novamobile-api.onrender.com
